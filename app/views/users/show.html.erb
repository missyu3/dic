<div class="user_show">
  <h1>マイページ</h1>
  <p><%= @user.name %></p>
  <p><%= @user.email %></p>
  <%= link_to "修正", edit_user_path, class: 'btn btn-success' %>
  <%= link_to "削除", user_path(@user.id), method: :delete, data: { confirm: "削除しますか？"}, class: 'btn btn-danger' %>
</div>
